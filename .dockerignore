# Context hygiene for container/Dockerfile's COPY . /opt/claude-sandbox.
# The devcontainer build (root Dockerfile) has no COPY, so this file does
# not change its behaviour.
.git
build
.venv*
tmp
node_modules
